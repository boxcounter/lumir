export const EVENT_DICT_DOC = "---\ndoc-id: synthetic-m115-fixture\nupdated: 2026-01-15\n---\n\n# 遥测事件登记册（合成示例）\n\n> 本文件是自动化测试专用的合成数据，描述虚构产品 Acme Clips 的遥测事件口径。文档结构仿照常见的事件登记类文档（多张表格、上方一张巨型标准属性表、下方一张事件表），所有公司、产品、表名、字段、枚举、流程、日期均为虚构，不对应任何真实系统或真实文档。\n\n登记口径：事件、属性、枚举取值的定义一律以本册为准；埋点实现、自动校验、变更登记都引用本册编号。任何与本册不一致的实现，以实现方修改为准入流程，不允许先斩后奏。\n\n存储口径：本文出现的 `ac_*` 均为虚构分析库表名（如 `ac_user_clip`），model 名与表名一致，不做映射；真实项目里常见的表名前缀、库名约定在本文一律不出现。\n\n编号口径：文中「规则 X#」编号（如规则 B4）纯属虚构示例，仅用来演示交叉引用的书写格式，不指向任何真实规范。规则之间的引用关系（B4 引用 B1、C2 引用 C1 等）也是虚构的。\n\n## 问题登记册\n\n领域事件必须挂在下列编号问题上（流程见「变更流程」一节）。登记新问题、退役旧问题都直接改这张表，并在「历史变更记录」补一行。\n\n问题描述统一用「人群 + 行为 + 比例 / 分布」的句式，保证可计算；决策用途一栏必须写清「这个答案会改变什么决定」，写不出的问题不登记——没有决策牵引的埋点是纯成本。\n\n每个问题由一份 tracking plan 实例化为完整的事件设计（规则 C2），plan 收录在 plans/ 目录（当前只有 plans/P1-新手激活）。plan 按编号逐个推进，目前只完成了 P1，P2–P5 尚未启动；词典里 P2–P5 的事件已经登记生效，实现侧可以照常开发、上报，等 plan 启动时直接取用。\n\n问题退役的判据也写在这里：连续两个季度没有决策引用该问题的答案，或对应功能线下线，问题转 retired；挂在它上面的事件同时进入退役流程（规则 C3），不再要求新需求兼容，但历史数据保留、词典保留登记。\n\n| 编号 | 问题原文 | 决策用途 | 提出人/日期 | 状态 |\n| --- | --- | --- | --- | --- |\n| P1 | 注册成功的用户里，首日完成一次网页收藏的比例是多少？未收藏的用户流失在哪个环节？ | 选择新手引导的重心：先推扩展安装，还是先把收藏入口做显眼 | 产品组 / 2026-01-05 | active |\n| P2 | 合集公开页的访客里，最终点了订阅的比例是多少？ | 评估合集页能否当作获客渠道加大投入 | 产品组 / 2026-01-07 | active |\n| P3 | 订阅了合集的用户里，每周真正打开阅读的比例是多少？ | 验证订阅关系是否持续交付价值，决定订阅体验的预算 | 产品组 / 2026-01-07 | active |\n| P4 | 完成首次收藏的用户里，七天内回访阅读的比例是多少？ | 选择留存抓手：加深单次消费，还是做回访触发 | 产品组 / 2026-01-09 | active |\n| P5 | 打开公开快照页（`/p`）的访客里，一周内注册的比例是多少？ | 评估单篇快照页作为获客入口的价值 | 产品组 / 2026-01-09 | active |\n\n## actor 词表\n\n- **user**：注册用户。与 object `user` 同指：actor 是行为的施加方，object 是承受方（例如 user 触发 `user_registered`）。\n- **visitor**：匿名访客，只有采集侧下发的 `device_id`；visitor 与 user 的合并规则见「采集与事件仓」一节。\n- **system**：服务端自发事件（规则 B3），例如定时任务产生的状态迁移。\n- **落位约定**：actor 不作为独立字段落库、也不写进 properties——它是词典层的标注，每个事件的 actor 在本册固定。数据里 user 与 visitor 靠 `user_id` 列区分（0 表示匿名）；`actor: system` 的事件由词典定义识别，必须携带的关联用户 id 写 `user_id` 列、关联 object id 写 `properties`（规则 B3）。\n\n三种 actor 的划分是口径问题而不是技术问题：同一个动作（例如定时任务把过期合集置为不公开）由人触发还是由系统触发，会进入完全不同的分析路径。登记时先定 actor，再定触发时机的文字描述，两者必须互相印证。\n\n## 属性三类落位速查\n\n登记字段之前，先判定它描述的是什么（规则 B1）：\n\n- **事件属性**：这一次发生的具体情况——各 object 的「标准属性」（登记在对应 object 条目的事件表上方；没有全域适用项的 object 也要显式写「无」，如 user）以及事件表的「事件专有属性」列。这类字段进 payload，是本词典的主体内容。\n- **上下文属性**：每次必带的环境信息——`platform` / `locale` / `app_version` 三项，由采集 SDK 自动附加，各事件表不再重复列出。\n- **用户维度**：描述「人」的字段（如 `ac_user.created_at`、`ac_user_plan.plan_code`）。它们不是事件字段、不落事件表；分析期由查询侧从业务库取数后按 user 合并，按设计不进入本词典。\n- 采集侧附加字段（原始 UA、`received_at` 等）：采集端点自行附加、写入者不感知，同样不在事件表中登记（见「采集与事件仓」一节）。\n\n判定拿不准时按高代价处理：先当事件属性登记、评审时再降级，而不是先漏掉再补——补字段要回填历史数据，成本远高于评审时删掉一个多余字段。\n\n## 事件命名规范\n\n- 事件名一律采用 `object_verb` 过去式（规则 A1），例如 `collection_followed`；禁止 `follow_collection` 这类动宾语序。\n- UI 家族事件（`*_viewed` / `*_clicked`）只描述界面的暴露与交互，不带 object 语义，回答的是「用户经过了哪些界面、操作了哪些元素」。\n- 领域 action 事件必须携带 object id，回答的是「哪个内容被做了什么」。同一个用户动作可以同时产生两类事件：一类用于内容计数，一类用于路径诊断，二者分工不同，不算重复上报。\n- 新事件先登记、后实现（规则 C1）：未登记的事件名会被自动校验拒绝，写入端拿到的是明确错误。\n- 事件名一旦入库不再改名（规则 C3 的推论）；要改语义就立新事件、退役旧事件。\n- 同一个动词在同一 object 下只表达一种语义：不要在不同事件里复用 `entered` 同时表示「打开阅读视图」和「打开设置页」；后者属于 UI 家族，应该用 `screen_viewed` 的 `screen_name` 区分。\n- 否定态与正向态成对登记（`hidden` / `unhidden`、`pinned` / `unpinned`），不允许只登记一个方向——只有一个方向的事件会让状态推导出现盲区。\n- object 名一律单数小写（`bookmark`、`collection`、`mark`），动词一律过去式（`saved`、`hidden`、`purged`）；复合动词用下划线分词（`save_rejected`、`read_entered`）。\n\n## 埋点实现 checklist（虚构示例）\n\n实现一个已登记的事件时，按以下顺序自查；任何一步不过，不提交评审：\n\n1. 事件名、actor、写入者与本册一致；触发时机与「触发时机」列的文字描述可以在日志里一一对应。\n2. 标准属性逐项携带；没有值的字段上报缺省而不是省略 key。\n3. 事件专有属性的枚举取值已在枚举登记表落地；临时取值先登记再上线。\n4. 上下文三项（`platform` / `locale` / `app_version`）由 SDK 自动附加，写入端不重复设置。\n5. 本地用采集侧提供的校验脚本跑一遍正负用例：正例能通过，未登记事件名被拒收。\n6. 触发点代码里留一行注释，引用本册的事件名与版本日期，方便半年后的人定位口径。\n7. 评审通过、灰度上线之后，回写事件表的 status 与「历史变更记录」。\n8. 上线后一周，看一遍数据质量监控里该事件的量与字段完整率，确认没有静默失败。\n9. 触发时机列的描述写到「能在日志里找到对应行」的精度；实现同学按描述打日志，评审同学按日志核对描述，两边对不上就打回。\n10. 涉及新枚举取值时，取值、含义、示例三者一起登记；只有取值没有语义的登记视为未完成。\n\n## Object 条目\n\n### user\n\n用户自身的状态变更（注册、登录、资料、设置）。纳入判据：数据模型的落点在 user 表（含扩展表与设置表）；用户针对内容做的操作归各自的 object（例如收藏网页归 bookmark）。\n\nuser 条目刻意保持最小：只有两个生命周期事件。资料、设置类的变更没有登记为事件——它们不产生「这一次发生」的语义，而是对当前状态的覆盖；分析侧需要这类信息时，直接读业务库的当前值与审计日志，不走事件流。这个判据（状态覆盖 vs 事件发生）适用于所有 object：凡是回答「现在是什么样」的，进维度表；凡是回答「什么时候发生了什么」的，才进事件表。\n\n**标准属性**：无全域适用项——`method` 只覆盖注册 / 登录两个事件，因此降为事件专有属性。\n\n| 事件 | actor | 写入者 | 触发时机 | 事件专有属性 | answers_question | status |\n| --- | --- | --- | --- | --- | --- | --- |\n| `user_registered` | user | server | 注册事务成功提交 | `method`（枚举：`email` / `sso`） | P1 | proposed |\n| `user_session_started` | user | server | 登录会话建立之后 | `method`（枚举：`email` / `sso`） | P1 | proposed |\n\n### bookmark\n\n用户收藏的一个网页。同一网页可以被很多人收藏：`ac_clip` 是内容实体，`ac_user_clip` 是「某用户收藏了某网页」的关系记录；收藏、隐藏、置顶、删除这些事件描述的都是某个用户对自己那条关系记录的操作，所以事件一律挂在关系（`ac_user_clip`）上，而不是挂在内容实体上。\n\n这样设计的好处：内容侧（`ac_clip`）只保存网页本身的信息，与用户行为解耦；统计「某网页被收藏多少次」时数关系行即可，不需要在内容行上维护计数。代价是事件查询一律要 join 关系表，分析侧的口径文档里要写清楚。\n\n需要注意的边界：同一用户对同一网页重复收藏时，关系记录只保留一条（重复收藏不产生新行），第二次收藏触发的是收藏事件的重放语义——写入端按幂等处理，不产生第二条关系记录，但事件照发，由分析侧决定是否去重。这个口径写在词典里而不是实现里，是因为「重复收藏算不算一次激活」是产品问题而不是工程问题。\n\n**标准属性**（所有 bookmark 事件继承）：\n\n| 属性 | 枚举取值 / 备注 |\n| --- | --- |\n| `clip_ref` | 关系记录 id，一律上报关系侧 id 而非内容侧 id |\n| `host_domain` | 只上报域名段——现阶段的问题只需要域名粒度（规则 B5）；确需完整 URL 时按规则 C4 新增字段，不复用本字段 |\n| `origin` | 枚举：`ext_popup` / `share_sheet` / `web_button` / `import` / `api` / `cli` / `starter_guide`（`ext_popup` = 浏览器扩展里的页内保存弹层，组件代号 SaveBox，扩展本身不提供独立弹出页；`cli` = 命令行客户端的保存入口；`starter_guide` = 新手引导期间、在示例文章上完成的首次收藏演练——载体仍然是扩展保存弹层，这个取值用来标记「由演练促成」，是 P1 激活判定的关键区分；功能未建，待引导流程上线时配套实现） |\n| `payload_kind` | 枚举：`full_snapshot`（带完整正文快照，例如扩展收藏的文章页）/ `link_only`（只存链接，例如手动添加入口）；对应 `ac_user_clip.kind`（虚构示意：0=文章 / 1=纯链接） |\n\n| 事件 | actor | 写入者 | 触发时机 | 事件专有属性 | answers_question | status |\n| --- | --- | --- | --- | --- | --- | --- |\n| `bookmark_saved` | user | server | 收藏关系记录创建事务提交成功 | — | P1 | proposed |\n| `bookmark_save_rejected` | user | server | 保存请求被拒绝，或处理流水线某个 step 失败 | `error_class`（枚举见表下口径注） | P1 | proposed |\n| `bookmark_read_entered` | user / visitor | client | 阅读视图或公开快照页（`/p`）打开 | `entered_from`（枚举与客户端判定口径见表下注） | P1（user，示例文章打开率）/ P3（`digest_collection`）/ P5（visitor） | proposed |\n| `bookmark_hidden` | user | server | `hidden_at` 置位的事务提交 | — | P4 | proposed |\n| `bookmark_unhidden` | user | server | `hidden_at` 被清除的事务提交 | — | P4 | proposed |\n| `bookmark_pinned` | user | server | `pinned_at` 置位的事务提交 | — | P4（策展侧问题登记后可双挂） | proposed |\n| `bookmark_unpinned` | user | server | `pinned_at` 被清除的事务提交 | — | P4 | proposed |\n| `bookmark_purged` | user | server | 用户发起删除：软删进回收站，或直接物理删除（端点与回收站机制见表下注） | — | P4 | proposed |\n\n> `error_class` 口径：只收录 server 可观测的失败——请求级（保存被拒）与 step 级（沿用处理流水线的 step_name 取值，如 `fetching` / `parsing` / `embedding`；取值清单在实施时从日志固化，新增 step 按只增流程登记）。客户端断网这类 server 观测不到的情况不算本事件（规则 B4 的写入者边界）；保存链路没有付费墙，所以 `paywall` / `network` / `auth_expired` 之类一律不登记。\n>\n> `entered_from`（bookmark）枚举与客户端判定：`library` = 书签列表（`/library` 各 tab）/ `digest_collection` = 收件箱里的合集视图 / `search_result` = 书签列表的页内搜索态（没有独立搜索路由）/ `direct` = 没有应用内入口的直接打开（冷启动 `/p/`、外部链接、手输 URL）。客户端判定方法：应用内导航按路由栈来源取前三个取值，没有应用内来源（冷启动）一律归 `direct`；`/p/` 页刷新会丢掉入口上下文，重记为 `direct`，这是已知的含糊，按现状记录。\n>\n> `bookmark_purged` 端点与回收站机制：进回收站走软删除（置 `deleted_at`），直接删除走物理删除端点（例如扩展里取消收藏）；回收站内容满 7 天由定时任务彻底清除，属内部维护，不发事件；从回收站恢复暂时没有对应事件。\n\n### collection\n\n策展合集（`ac_collection` 是合集定义、`ac_collection_subscriber` 是订阅关系；被星标的合集自动组成公开合集页 `/c/[uuid]`）。\n\n**标准属性**：`collection_id`\n\n| 事件 | actor | 写入者 | 触发时机 | 事件专有属性 | answers_question | status |\n| --- | --- | --- | --- | --- | --- | --- |\n| `collection_entered` | user / visitor | client | 合集页从任一入口打开（公开页或收件箱合集列表） | `entered_from`（枚举：`external_link` / `digest`） | P2（visitor）/ P3（user） | proposed |\n| `collection_followed` | user | server | 订阅关系记录创建事务提交 | — | P2 | proposed |\n| `collection_unfollowed` | user | server | 订阅终止事务提交 | — | P3 | proposed |\n\n> `collection_tier` 与 `follow_tier` 都不登记：当前合集只有免费形态，这两个属性没有区分度。将来付费形态回归时按规则 C4 再登记。\n>\n> `collection_unfollowed` 的语义：订阅关系一终止就发本事件——当前只有免费形态，终止即删除 subscriber 行。\n\n### mark\n\n划线（`ac_clip_note` type=1；object 名用 mark，与评论区分）。\n\n**标准属性**：`mark_id`、`clip_ref`\n\n| 事件 | actor | 写入者 | 触发时机 | 事件专有属性 | answers_question | status |\n| --- | --- | --- | --- | --- | --- | --- |\n| `mark_created` | user | server | 划线写入事务提交 | — | P4 | proposed |\n| `mark_erased` | user | server | `is_erased` 置位事务提交 | — | P4 | proposed |\n\n## 候补与排除\n\n**候补事件**（挂接的问题缺失，暂不进正表）：`user_profile_updated`（用户资料类字段的变更；维度字段的取值必须先在枚举登记表落地）。\n\n**候补 object**：`importer` / `ai_digest` / `plan` / `note`。\n\n**排除的语义**：`snooze`（稍后提醒）——虚构设定中产品从未实现该特性，属冗余状态；不登记，代码清理另行安排。\n\n**排除**：`notify`（示例中现阶段不关心）、`share`（虚构 legacy：快照页公开后复制链接即完成分享，独立的 share 事件已无意义）、`tag`（示例中非核心功能）。\n\n**排除的枚举取值**：`origin=agent_api`（虚构设定：代理路由 prod 无绑定、工具只读产生不了保存；将来代理接口开放写操作后按规则 C4 重新登记）。\n\n## 全量事件索引（虚构示例，供检索）\n\n| event_name | object | actor | writer | answers_question | status |\n| --- | --- | --- | --- | --- | --- |\n| user_registered | user | user | server | P1 | proposed |\n| user_session_started | user | user | server | P1 | proposed |\n| bookmark_saved | bookmark | user | server | P1 | proposed |\n| bookmark_save_rejected | bookmark | user | server | P1 | proposed |\n| bookmark_read_entered | bookmark | user / visitor | client | P1 / P3 / P5 | proposed |\n| bookmark_hidden | bookmark | user | server | P4 | proposed |\n| bookmark_unhidden | bookmark | user | server | P4 | proposed |\n| bookmark_pinned | bookmark | user | server | P4 | proposed |\n| bookmark_unpinned | bookmark | user | server | P4 | proposed |\n| bookmark_purged | bookmark | user | server | P4 | proposed |\n| collection_entered | collection | user / visitor | client | P2 / P3 | proposed |\n| collection_followed | collection | user | server | P2 | proposed |\n| collection_unfollowed | collection | user | server | P3 | proposed |\n| mark_created | mark | user | server | P4 | proposed |\n| mark_erased | mark | user | server | P4 | proposed |\n| screen_viewed | ui | user / visitor | client | all | proposed |\n| element_clicked | ui | user / visitor | client | all | proposed |\n\n## 枚举登记表\n\n| 枚举 | 取值 | 备注 |\n| --- | --- | --- |\n| `screen_name` | `library` / `intro` / `digest` / `reader` / `save_popup` / `join` / `join_done` | 概念屏，跨端全局，禁止端前缀；与实际载体的映射关系见下一节 |\n| `element_id` | `shelf_empty_cta`（screen: library）/ `intro_save_cta` / `intro_finish_cta`（均 screen: intro）/ `gate_install_cta` / `gate_dismiss`（均 screen: join_done）；status: proposed | 全部为虚构元素：引导页 hero 区的两个按钮、注册完成页的两个门控按钮、列表空态的一个引导按钮。登记日期：intro 两元素 2026-01-10、门控两元素 2026-01-12；空态按钮未留登记日期（框架建立期） |\n| `ui_action` | `viewed`（screen 专用）/ `clicked`（全端）/ `hovered`（web + extension） | 每个取值须标注适用平台 |\n| `origin`（bookmark） | 见 bookmark 标准属性 | `starter_guide` 是未建取值 |\n| `entered_from` | bookmark 与 collection 各自有一组取值，见各自条目 | 属性名共用，取值按 object 分别登记 |\n| `error_class` / `method` / `payload_kind` | 见各 object 条目 | — |\n\n### screen_name 概念屏 → 实际载体映射\n\n概念屏是跨端抽象（规则 A3），跟实际路由 / 载体不是强一致关系；实施与核查时对照明细如下：\n\n| 概念屏 | 实际载体 |\n| --- | --- |\n| `library` | Web `/library`（alias `/`） |\n| `intro` | 没有 `/intro` 路由；安装扩展后实际打开 `/guide?from=extension`。本屏登记的 `intro_save_cta` / `intro_finish_cta`，其 DOM 实体在引导页 hero 区。安装后引导下个版本才上线，本屏事件随其就绪 |\n| `digest` | `/library` 的收件箱 tab（不是独立路由） |\n| `reader` | 阅读页 `/p/[uuid]`（`/library/[id]` 只是重定向跳板；代码里的惯用称呼是 reader / article，组件代号 ReaderView，i18n key `pages.reader`） |\n| `save_popup` | 扩展的页内保存弹层（组件代号 SaveBox，代码里叫 SaveBoxPanel；点击扩展图标或按 Alt+Y 触发）。扩展既没有 popup 页也没有 options 页——右键菜单的「设置」实际打开 Web `/account` |\n| `join` | 没有独立注册页；注册和登录合并在 login 页 |\n| `join_done` | 门控式注册完成页（虚构设定为未建，status: proposed） |\n\n## 变更流程\n\n- 新增、修改、退役事件与字段，必须先在本册登记（规则 C1），评审通过后再实现；实现落地后回写 status。\n- 字段只增不改（规则 C3）：语义变化走新字段，旧字段标 deprecated，并在备注里写明迁移窗口。\n- 枚举取值只增不删（规则 C4）：下线的取值保留登记并标注停用日期，避免历史数据无法解读。\n- 每季度把埋点实现和本册核对一次：以本册为准出差异清单，修复排进下一个迭代。\n- 每次变更在「历史变更记录」补一行，写清日期、变更内容、涉及事件、执行人；缺记录按未变更处理。\n- 本节规则编号（A1 / A3 / B1 / B3 / B4 / B5 / C1 / C2 / C3 / C4）都是虚构示例，仅演示交叉引用格式。\n\n## 术语表\n\n- **payload**：事件上报体，包含事件属性与上下文属性，不含用户维度。\n- **step**：保存处理流水线中的一个环节（fetching / parsing / embedding），每个 step 有独立的成功与失败日志。\n- **tracking plan**：一个问题的完整事件设计文档，引用本册的事件与字段，落盘后挂在 plans/ 目录。\n- **概念屏**：跨端的界面抽象，用于 `screen_name`；与实际路由的映射见上文对照表。\n- **关系记录**：用户与内容实体之间的关联行（如 `ac_user_clip`），bookmark 事件一律挂在关系记录上。\n- **回收站**：软删除的暂存区，7 天后由定时任务彻底清除；清除动作属内部维护，不产生事件。\n- **演练**：新手引导期间引导用户完成的首次收藏，用 `origin=starter_guide` 标记，与自然收藏区分。\n- **写入者**：事件由哪一端写入（server / client）；写入者决定可观测性边界（规则 B4）。\n- **事件仓**：采集事件的统一存储；业务语义以本册为准，仓内不做二次解释。\n- **缺省上报**：字段无值时上报约定的缺省值（空串 / 0 / `unknown`）而不是省略 key，保证下游 schema 稳定。\n- **回填**：匿名事件在用户注册或登录后按合并规则归入 user 的过程；窗口期见「采集与事件仓」。\n- **门控**：注册完成前的阻塞式界面（虚构设定），其上元素单独登记 `element_id`。\n\n## 数据质量监控（虚构示例）\n\n- 每日对账：事件量波动超过 3 倍标准差时出告警，按事件名定位到写入者。\n- schema 校验：写入端与采集端各自校验一次；未登记的事件名、未登记的枚举取值直接拒收并计数。\n- 抽样回放：每周抽取 0.1% 的事件回放校验字段完整性，缺失率超过阈值时冻结对应事件的 status。\n- 维度退化监控：`entered_from=direct` 占比异常升高时，优先怀疑入口上下文丢失（见 bookmark 表下注）。\n- 对账结果每周汇总到质量看板，连续两周红灯的事件冻结新需求，先修数据。\n- 告警分级：量归零为 P0（当天处理）、字段缺失率超阈为 P1（当迭代处理）、分布漂移为 P2（季度核对时处理）。\n\n## 历史变更记录（虚构示例）\n\n| 日期 | 变更 | 涉及事件 / 字段 | 执行人 |\n| --- | --- | --- | --- |\n| 2026-01-05 | 初版登记：user / bookmark / collection / mark 四个 object | 全部 P1 事件 | 数据组 |\n| 2026-01-07 | P2 / P3 问题登记，补充 collection 三事件 | `collection_entered` 等 | 数据组 |\n| 2026-01-09 | P4 / P5 问题登记，补充 bookmark 状态事件 | `bookmark_hidden` 等 | 数据组 |\n| 2026-01-10 | intro 概念屏两个元素登记 | `intro_save_cta` / `intro_finish_cta` | 客户端组 |\n| 2026-01-12 | 门控注册完成页两个元素登记 | `gate_install_cta` / `gate_dismiss` | 客户端组 |\n| 2026-01-13 | 数据质量监控分级口径落地 | 无（流程项） | 数据组 |\n| 2026-01-14 | 全量事件索引表上线，供检索 | 无（索引项） | 数据组 |\n| 2026-01-15 | 排除 `snooze` / `notify` / `share` / `tag`，注明排除理由 | 无（排除项） | 数据组 |\n\n## 上报时序与重试（虚构示例）\n\n- 客户端事件先入本地队列，网络可用时批量上报；上报失败按指数退避重试，超过 48 小时丢弃并计数。\n- server 事件在事务提交后由 outbox 转发，至少一次送达；消费侧按 `event_id` 去重。\n- 时间戳一律取事件发生端的本机时间，采集侧记录 `received_at` 作为到达时间；两端偏差超过 5 分钟时以到达时间为准参与分区。\n- 本节机制均为虚构示例，仅用于演示词典文档中「时序与重试」一节的写法。\n\n## 采集与事件仓（虚构示例）\n\n- 采集端点只负责接收、校验、附加上下文，然后写入事件仓；业务语义一律以本册为准。\n- 匿名态用 `device_id` 标识；注册或登录后按合并规则把匿名事件回填到 user（规则 B3），回填窗口是注册前 30 天。\n- 数据边界：事件负载不含正文内容、不含页面截图、不含表单输入；内容类字段一律只带 id 与枚举（规则 B5）。\n- 合并规则按「先 device 后 account」：同一 `device_id` 的匿名事件在首次登录后回填；多设备场景按设备逐一回填，不做跨设备合并。\n- 本节的合并窗口、字段清单、目录结构均为虚构示例，不对应任何真实数据管道。\n\n## 常见问题（虚构 FAQ）\n\n- **为什么没有内容计数字段？** 计数由事件聚合得到，落库冗余字段会引入一致性问题（规则 B1 的推论）。\n- **为什么 `origin` 取值里保留了未建的 `starter_guide`？** 先登记后实现（规则 C1）：引导流程上线时写入端直接可用，不需要二次变更词典。\n- **客户端断网产生的事件怎么办？** 客户端事件允许延迟上报并带原始时间戳；server 观测不到的失败不建模为事件（见 `error_class` 口径注）。\n- **可以临时加一个「先用着」的字段吗？** 不可以。先登记再实现没有例外；评审平均耗时一个工作日，不是瓶颈。\n- **历史数据里出现未登记取值怎么处理？** 按数据质量事故处理：冻结相关事件，补登记或清洗，写进历史变更记录。\n- **visitor 与 user 的合并会丢数据吗？** 窗口期（注册前 30 天）内的事件全部回填；窗口期外的匿名事件保留在匿名分区，不删但也不归并。\n- **事件的 answers_question 可以改吗？** 可以，属于词典层标注，改挂不影响已上报数据；但要在历史变更记录里写明原因。\n\n## 反例集（虚构示例）\n\n以下命名与登记方式一律禁止，评审见到直接打回：\n\n| 反例 | 问题 | 正确写法 |\n| --- | --- | --- |\n| `save_bookmark` | 动宾语序，违反规则 A1 | `bookmark_saved` |\n| `BookmarkSaved` | 大小写混用 | `bookmark_saved` |\n| `bookmark_save` | 动词非过去式 | `bookmark_saved` |\n| `bookmark_clicked_save` | 把 UI 动作写进领域事件 | UI 侧用 `element_clicked` |\n| `user_delete_bookmark` | object 错位，actor 混入事件名 | `bookmark_purged` |\n| `collection_sub` | 缩写不可读 | `collection_followed` |\n| `mark_add` | 动词与全集不统一（add vs created） | `mark_created` |\n| `bookmark_saved_v2` | 版本后缀 | 语义变更走新事件名并退役旧事件 |\n\n## 字段类型约定（虚构示例）\n\n| 类型 | 表示 | 示例 | 备注 |\n| --- | --- | --- | --- |\n| id | string | `clip_ref` | 一律字符串，不上报数值 id |\n| enum | string | `origin` | 取值必须先登记 |\n| bool | int (0/1) | `is_erased` | 仓内统一 0/1，不用 true/false |\n| ts | int (unix ms) | `received_at` | 毫秒精度，UTC |\n| count | int | `retry_count` | 非负整数 |\n| text | string | `error_detail` | 长度上限 256，截断标记 `…` |\n| ratio | float | `sample_rate` | 0..1 闭区间 |\n| version | string | `app_version` | semver 三段式 |\n| locale | string | `locale` | BCP-47 代码 |\n| platform | string | `platform` | 枚举见登记表 |\n| json | string | `properties` | 序列化后的 JSON 文本 |\n| url_part | string | `host_domain` | 只保留域名段（规则 B5） |\n| duration_ms | int | `fetch_elapsed` | 非负整数 |\n| device | string | `device_id` | 采集侧下发 |\n\n## 灰度与回滚（虚构示例）\n\n- 新事件先灰度 5% 流量上报，观察一天数据质量监控无告警，再全量。\n- 灰度期间事件的 status 标 `trial`，全量后回写 `proposed`。\n- 回滚只停上报，不改词典；词典中的事件保留登记并标注暂停原因。\n- 回滚超过一个迭代未恢复的，按退役流程处理（规则 C3），避免长期悬挂。\n- 灰度与全量之间不允许改字段；要改就先回滚、再按变更流程走。\n\n## 端口径对照（虚构示例）\n\n| 口径 | web | extension | cli | server |\n| --- | --- | --- | --- | --- |\n| `origin=ext_popup` | — | yes | — | — |\n| `origin=web_button` | yes | — | — | — |\n| `origin=cli` | — | — | yes | — |\n| `origin=api` | — | — | — | yes |\n| `entered_from=library` | yes | — | — | — |\n| `entered_from=direct` | yes | yes | — | — |\n| `ui_action=hovered` | yes | yes | — | — |\n| `ui_action=clicked` | yes | yes | yes | — |\n\n## 参考实现注记（虚构示例）\n\n- 写入端统一走采集 SDK 的 `track(event, props)`；不允许绕过 SDK 直接写事件仓。\n- SDK 负责附加上下文三项、生成 `event_id`、本地队列与重试；业务代码只关心事件名与事件属性。\n- 校验脚本以本册为输入生成，词典变更后自动更新；写入端与采集端使用同一份生成物，避免双份维护。\n- 本节目的只是演示词典文档中「参考实现注记」一节的写法，所有组件名、脚本名均为虚构。\n\n## 审计日志格式（虚构示例）\n\n| 序号 | 字段 | 取值示例 | 说明 |\n| --- | --- | --- | --- |\n| 1 | audit_id | `aud_01J4ZXYZ…` | ULID，全局唯一 |\n| 2 | actor | `data-team` | 变更执行组 |\n| 3 | action | `register_event` | register / deprecate / retire / amend |\n| 4 | target | `bookmark_saved` | 事件名或字段名 |\n| 5 | before | `null` | 变更前状态（amend 时非空） |\n| 6 | after | `proposed` | 变更后状态 |\n| 7 | reason | `P1 plan kickoff` | 变更理由，必填 |\n| 8 | created_at | `2026-01-15T09:30:00Z` | RFC 3339 时间戳 |\n\n## 评审记录模板（虚构示例）\n\n每次登记评审在 plans/ 目录留一份记录，包含：参会人、评审结论（通过 / 打回 / 有条件通过）、逐条意见、后续行动项与截止时间。打回的意见必须写明依据的规则编号；有条件通过的，条件本身也登记为行动项并跟踪到关闭。评审记录与「历史变更记录」一一对应，缺其一视为流程未完成——这条约定让每个事件都能回溯到「谁、在什么时间、基于什么理由」让它进入词典。\n\n## 与其他文档的关系（虚构示例）\n\n- 本册只定义「有什么事件、字段什么意思」；「怎么用这些事件回答业务问题」由 tracking plan 文档承担。\n- 采集 SDK 的使用说明、写入端的接入指南由工程文档承担，本册不重复；两边通过校验脚本生成物保持一致。\n- 数据质量看板的使用说明由数据组内部文档承担；本册只约定监控口径（告警分级、冻结规则），不约定看板形态。\n- 以上划分是虚构示例，用于演示词典文档末尾「文档关系」一节的写法。\n";
