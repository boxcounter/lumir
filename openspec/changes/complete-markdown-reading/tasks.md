# Tasks: complete-markdown-reading

节点 1 已由 tower 依 Alex 授权于 2026-09-06 通过（M71 独立 r1 clean）；仅条件批准 BlockWrapper 候选路线。真实 WK 前置实验通过前不进入完整表格实现，节点 2 与后续验收保持未完成。

## 1. 提案评审与可行性

- [x] 1.1 向 Alex 展示 proposal、design 与 spec，取得节点 1 的明确裁决并记录结果。tower 代行批准三项边界，详见 proposal Review Decisions。
- [ ] 1.2 使用现有 parser 固定列表/矩形表格范围 fixture；核实空槽、转义、非矩形、嵌套列表/引用等边界。
- [ ] 1.3 在真实 Tauri WKWebView 验证公开 BlockWrapper/line/cell decoration 的宽表、虚拟视口、空槽、选择复制与可访问语义；不通过则停止并修订方案重新评审，不以全量 widget 绕过。

## 2. 列表与表格阅读

2026-09-06 tower 依 Alex 本轮授权对 M73 作出具体列表取舍：拒绝固定 `14.5ch` 的短列表容量，采用实际标记紧凑宽度；允许不完整组元数据暂呈源码，异步完整后一次切最终对齐，完成后滚动/选区稳定。详见 design 决定 1；本次仅记录裁决与待评审约束，不代表修订路线已通过 review。

- [ ] 2.0 对列表紧凑宽度、partial→complete 一次切换及完成后稳定的修订 design/spec 进行独立 review；通过后才实施该路线。
- [ ] 2.1 实现同级标记区与正文起点布局，按组实际最长原始标记及必要间距紧凑计算，覆盖普通/任务混排及嵌套；组元数据未完整时暂呈源码，异步完整后一次切最终对齐，不同步强制全组解析、不永久回退。验证可控 partial→complete 最终宽度、仅末端出现任务/最长编号时不重叠、短编号窄栏标记包围盒及剩余正文宽度，以及完成后滚出/滚入、中部定位和选区变化稳定（误差不超过 1 CSS px）。
- [ ] 2.2 实现软换行和源码正文续行悬挂；隔离代码块及非列表段落，处理字体/窗口变化。
- [ ] 2.3 实现矩形 pipe 表格行列、空槽和源码列对齐；异常或不支持结构回退源码。
- [ ] 2.4 实现表格局部横向滚动、键盘焦点进出与表头/单元格可访问语义。
- [ ] 2.5 接入视口增量及缓存失效，防止表格与既有行内装饰重复消费，不改文档或 code 模式。
- [ ] 2.6 保证整表、跨表、局部选区与全选复制映射到源码，保持任务点击及其他输入只读。

## 3. 验证

- [ ] 3.1 `npx --yes @fission-ai/openspec@1.12.0 validate --all --strict` 通过。
- [ ] 3.2 `pnpm build` 和 `pnpm --dir tests/visual test` 通过；三套主题宽/窄窗口的列表对齐、嵌套、表格列对齐及末列可达断言通过。
- [ ] 3.3 真实 Tauri WKWebView 打开 fixture，验证触控板/键盘横滚、焦点进出、resize、滚出/滚入及表格中部定位，保存验收证据。
- [ ] 3.4 真实 WKWebView 鼠标/键盘选择与 Cmd+A/C 后粘贴至纯文本接收端，逐项比较整表/跨表/局部/全选源码；尝试输入、剪切、粘贴、Delete 和任务点击，比较内存及磁盘内容未变。
- [ ] 3.5 按 `.github/workflows/perf.yml` 执行四项测量与阈值检查；补测 1MB 多列表/多表、单个大表的打开和滚动，不放宽既有门禁或把占位测试视为真实场景证明。
- [ ] 3.6 检查 M1 只读、M2 源码编辑及非必需专用表格操作边界，确认未变更冻结原型、历史归档及其他里程碑裁决。
- [ ] 3.7 向 Alex 展示实现验收与 spec 增量，取得节点 2 归档裁决；仅裁决通过后另行 archive。
