## ADDED Requirements

### Requirement: vault 列表浮层的输入筛选

vault 切换器浮层 SHALL 支持输入筛选。**匹配语义 SHALL 与 `toc-outline` 的「浮层输入筛选的匹配语义」
同一套**（大小写折叠的子串匹配、顺序不重排、单一实现），**交互与退出口径 SHALL 与 `toc-outline` 的
「浮层输入筛选的交互（输入框、焦点与退出）」同一套**（输入框持焦点、`↑↓`/`⌃N`/`⌃P` 在结果集里移动、
`Enter` 执行该浮层的既有动作、`Esc` 一步关闭并丢弃查询、无命中保持浮层并给一行提示、焦点离开浮层即
收起）。本 capability MUST NOT 重述这两条规则、MUST NOT 另写一份匹配实现或第二套键位口径——本节只写
vault 侧的作用面。

匹配对象 SHALL 是 vault 的**显示名**（目录名）。MUST NOT 匹配行摘要（标签数 / 相对时间）、MUST NOT 匹配
路径尾部、MUST NOT 匹配失效行的成因文案：摘要是「多久没用过它」的读数、不是身份，参与匹配会让输入一个
数字（如 `1`）命中一堆无关行。**已知边界（如实记录）**：同名但位于不同目录的 vault 无法靠输入区分
（按路径匹配另属一件事）。

**失效行 SHALL 同样参与筛选**（按显示名）：它是「重新定位…」的唯一入口，筛选 MUST NOT 让它消失；命中
失效行后 `Enter` 与点击的动作 SHALL 保持现状（走重定位，不发起切换）。

浮层底部的「新增 vault…」入口与它上方的分隔线 SHALL NOT 参与筛选、MUST NOT 被筛掉：它不在列表里，
是浮层内唯一的新增入口；无命中时它 SHALL 仍可见可用（用户仍能新增一个 vault 来摆脱空结果）。

当前 vault 被筛掉时结果集里 SHALL 不出现当前项标记；MUST NOT 因此改变当前 vault、MUST NOT 触发任何
切换动作。结果集 SHALL 以「本次拉取到的条目数组的下标」表达，游标、当前项与动作落点 SHALL 在同一空间
解释。

「重新定位…」的占用判定 SHALL 仍以**本次拉取到的完整列表**为输入，MUST NOT 用筛选后的结果集：占用
判定要与注册表全貌比较，收窄它的输入会让被筛掉的 vault 不再参与判定，用户就能把一个已被别的 vault
占用的目录当成重定位目标（两个身份落到同一路径，D105 的拒绝与「重定位到已被占用的路径被拒绝」一起
静默失效）。

打开路径 SHALL 不变：列表每次打开重新拉取、排序由后端给出、前端不重排；筛选 SHALL 只作用于本次拉取到
的数组，MUST NOT 触发第二次拉取、MUST NOT 引入常驻镜像。

#### Scenario: 输入即筛（显示名）

- **WHEN** 浮层打开后输入 `s`
- **THEN** 列表只剩显示名含 `s` 的 vault（含首字母为 `s` 的与中段含 `s` 的），相对顺序与后端给出的
  顺序一致

#### Scenario: 摘要与时间不参与匹配

- **WHEN** 列表里有一行显示「3 个标签 · 2 分钟前」，输入 `分钟`
- **THEN** 该行不因摘要命中；只有显示名含该串的行才出现在结果集里

#### Scenario: 失效行参与筛选且 Enter 仍走重定位

- **WHEN** 某失效 vault 的显示名命中查询，用键盘选中它并按 `Enter`
- **THEN** 走既有的重定位链路（不发起切换、不打开任何 vault），与筛选前同一行按 `Enter` 的行为逐条一致

#### Scenario: 新增入口不受筛选影响

- **WHEN** 输入一个没有任何 vault 命中的串
- **THEN** 浮层保持打开，列表区给一行无匹配提示，「新增 vault…」入口仍可见可用，点击仍走既有的目录
  选择链路

#### Scenario: 当前项被筛掉不改当前 vault

- **WHEN** 当前 vault 的显示名不含查询串
- **THEN** 结果集里没有当前项标记，当前 vault 与右栏正文完全不变；清空查询后当前项标记回来

#### Scenario: 筛选不改变占用判定的输入集

- **WHEN** 用查询把某个 vault 筛出结果集，随后把一个**已被该 vault 占用**的目录当作另一个失效项的重
  定位目标
- **THEN** 该路径仍被判定为已占用并拒绝（与筛选前一致）——占用判定看的是完整列表，不是结果集

#### Scenario: 打开时仍只拉取一次

- **WHEN** 打开浮层后连续输入多个字符
- **THEN** 列表来源只被调用一次（输入只过滤本地数组，不触发第二次拉取）
