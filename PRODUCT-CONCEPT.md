# Lumir Product Concept v0.1

## 1. 产品定义

> **Lumir 是一个把 Agent 变成可见协作者的本地文本工作台。**

它服务于有大量知识文件、使用 Agent 进行知识创作和研究的个人。Lumir 不是 IDE、普通 Markdown 编辑器或聊天工具外挂；它让人和 Agent 在真实文件上持续进行阅读、理解、质疑、写作、修改、整理和内容形成。

核心命题：

> **把阅读中的认知摩擦，转化为正文演化。**

## 2. 产品动机

当前工作常常在 Obsidian 与外部 Agent 之间断裂：阅读中发现多处疑问后，需要复制片段、手工组织上下文、粘贴到 Agent 对话，再回到 Obsidian 人肉寻找原文并判断修改。

Lumir 要消除的是整段上下文搬运和结果回收过程：

```text
阅读
→ 在原文处留下编辑意图
→ 继续阅读，积累多个编辑点
→ 一次性交给 Agent
→ Agent 解释并直接修改正文
→ 按编辑点回看解释和新正文
→ 疑问消解
→ 归档
```

## 3. 目标用户

### 核心用户

- 方法论、文章和研究创作者；
- 管理大量个人工作记录的人；
- 使用 Codex、Claude Code、Kimi Code CLI 等 Agent 的人；
- 以本地文件管理知识的人；
- 经常在多个文件之间阅读、比较、整理和写作的人。

### 非核心用户

- 需要完整 IDE 能力的程序员；
- 只想随手记录 Markdown 的用户；
- 主要需要团队协作的人；
- 需要云端多人实时编辑的人；
- 需要复杂数据库和结构化知识管理的人。

## 4. 产品价值排序

### 不可牺牲

#### 极致美

美不仅是视觉装饰，还包括界面秩序、信息分寸、Agent 状态的清晰度、解释与修改的关系，以及用户每天打开它的意愿。Lumir 应该安静、清楚、有分寸。

#### Agent 协作深度

Agent 必须能够理解选区、文件、Thread 和相关文件；延续已有协作上下文；接收阅读中的编辑点；在明确授权后直接修改正文；解释修改原因；将修改准确带回原文；支持多轮追问；记录变更批次；避免静默覆盖人的编辑。

### 第一版可以暂时弱一些

- **极致快**：本地编辑循环必须流畅，Agent、索引和跨文件分析可异步、可稍慢；
- **中文输入与键盘体验**：先提供命令面板、常用快捷键和不破坏中文输入的交互，完整键盘体系后置；
- **文件自由**：先支持 Markdown、纯文本、常见代码、HTML、CSS、JSON 等文本格式，核心内容必须保持普通文件可读；
- **可扩展性**：先支持一个主要 Agent 接入和少量内置能力，但提前隔离未来的 Agent、文件、Thread、渲染和变更层。

### 明确不做

- 代码 LSP；
- 代码调试器；
- 大规模团队能力；
- 云同步；
- 社交协作。

## 5. 与其他产品的差异化

### 与 Obsidian

Obsidian 的核心单位是 Note，Lumir 的核心结构是 Thread 加文件。Obsidian 主要保存和组织知识；Lumir 组织正在发生的知识工作。Lumir 不与 Obsidian 竞争插件、图谱和所有知识管理功能，而是解决阅读问题、Agent 协作、跨文件上下文和修改回收。

### 与 Cursor / Zed

Lumir 面向文章、研究、方法论、工作记录以及混合文档和代码的知识空间。代码在 Lumir 中是可读、可改、可被 Agent 协作的文本材料，而不是需要 LSP、调试器和构建系统的软件项目。

### 与 Notion / Craft

Lumir 不把核心内容锁在云端数据库中，文件保持本地和开放；它不做团队、云同步和社交协作，换取本地文件自由和深度 Agent 协作。

### 与 ChatGPT / Claude

Lumir 将对话、原文、编辑意图、修改和回看放在同一个工作过程中，避免复制上下文、手工搬运结果和重新寻找原文。

### 与 Emacs / Vim

Lumir 借鉴键盘优先和高效操作，但不继承 Emacs 的复杂脆弱配置或 Vim mode 对中文输入的摩擦。配置是数据，不是程序；配置错误不能阻止编辑器启动。

## 6. 核心概念模型

```text
Workspace
  └── 真实文件和目录

Thread
  ├── 工作目标
  ├── 阅读集合
  ├── Thread brief
  ├── 编辑点
  ├── 变更批次
  └── 一个或多个 Agent Session

Session
  ├── 独立 context window
  ├── Agent / 模型
  ├── 工具权限
  ├── 对话历史
  └── 当前运行状态
```

### Workspace

Workspace 是材料边界，决定 Agent 可以在哪里查找、读取和操作文件。

### Thread

Thread 是显式的一等导航对象，代表“我正在用这些材料，与 Agent 一起完成什么”。它可以有主文档，也可以只是阅读或研究 Thread；可以关联多个文件、长期暂停、分叉和使用多个 Session。Thread 不拥有文件，也不是文件夹。

### Session

Session 是 Agent 的运行实例，拥有独立 context window、Agent / 模型、权限和运行状态。Thread 与 Session 不等同：Thread 是产品层的持续工作容器，Session 是运行时实例。每个 Thread 默认拥有独立 Session；Session 失效、上下文过长或更换 Agent 时，可根据 Thread brief 重建。

## 7. Thread 设计

### 显式导航

左侧展示 Thread 列表，包含标题、最近活动、开放编辑点、待回看变更和当前状态。默认采用紧凑列表，而不是卡片墙。

### 多文件阅读集合

Thread 可关联主文档、参考材料、来源 / 证据、草稿、输出候选、Agent 发现和待阅读文件。文件角色只属于当前 Thread，不改变文件本身；同一文件可以被多个 Thread 以不同角色引用。

### 无主文档 Thread

允许纯阅读和研究 Thread。它们可以产出阅读过的文件、研究摘要、结论、未解决问题或可转交给写作 Thread 的材料，不强制生成正文。

### Thread 首页

Thread 首页用于恢复上下文，而不是作为项目管理器。它显示目标、当前状态、最近活动、关联文件、开放编辑点、最近形成的判断和下一步建议。进入 Thread 后默认仍以阅读为主，而不是进入聊天界面。

### Thread 分叉与生命周期

同一 Workspace 中，目标改变时可以创建新 Thread，例如从“整理成文章”分叉出“整理成演讲稿”。新 Thread 可选择带入文件、术语和已确认结论，但默认不带入旧任务的全部聊天历史、未解决编辑点和临时目标。

Thread 只保留：

```text
进行中 / 暂停 / 完成 / 归档
```

Thread 不因长期无活动自动删除。

## 8. 编辑点

> **编辑点是一次性的认知事务：开始于阅读中的认知摩擦，结束于疑问消解和任务归档。**

### 创建

选中原文，使用快捷键或命令，输入自然语言意图：

```text
选中 → C-c ? → 这里的“访问”具体指什么？
```

编辑点记录原始引用、文件和位置、用户问题 / 意图、语义锚点、所属 Thread 和 Session。默认不插入正文，也不污染 Markdown 文件。

### 可表达的意图

- 理解：这里我不懂；
- 澄清：这里表达不清；
- 核验：这里需要证据；
- 对照：这和另一处是否矛盾；
- 扩展：这里值得补充；
- 压缩：这里太冗长；
- 改调：不像我的语气；
- 重构：这几段结构有问题。

用户不必先选择类型，直接描述即可。

### 一次性任务，多轮对话

```text
开放
→ 处理中
→ 待回看
→ 已解决
→ 归档
```

编辑点可以经历多轮对话和多次修改。完成条件不是 Agent 返回或文件改变，而是人认为这次阅读摩擦已经消解。有些编辑点最终只得到解释，不需要修改正文。

## 9. Agent 协作与授权

Agent 更像熟悉用户的编辑，而不是无状态聊天机器人。它可延续个人表达偏好、术语习惯、项目约定以及当前 Thread 的目标和读者。

记忆分层：

```text
个人偏好      跨 Thread
Workspace     项目内
Thread        当前工作目标内
Session       当前运行上下文内
```

原则：**编辑认识你，但不假装记得每一篇文章。**

### 直接修改正文

明确委派后，Agent 可以直接修改正文。授权边界由委派决定，而不是由修改大小决定：

```text
处理当前 4 个编辑点，允许修改相关段落；不改变文章结构。
```

Agent 不应因局部授权而扩大到整篇文章或整个 Workspace。大范围任务应形成明确的工作批次或候选版本。

### 协作底线

- 授权范围清楚；
- 不静默覆盖人的同期编辑；
- 修改前检查文件版本；
- 变更可解释、可回顾、可撤销；
- 文件冲突显式呈现；
- 未接入 Lumir 协作协议的外部 Agent 直接改盘时，不伪造协作状态。

## 10. Agent 返回与回看

用户偏好：**先看解释，然后读新正文。**

一次回看按编辑点进行：

```text
你的问题
→ Agent 的解释
→ Agent 做了什么修改
→ 修改影响或代价
→ 跳到真实正文阅读
→ 继续追问、手改、归档或下一处
```

解释应帮助人理解原文问题、Agent 判断、依据、变化和代价，而不是替修改辩护。Agent 可以明确表达不确定性并选择不修改。

Agent 完成后进入轻量逐项回看，但正文仍是主画面。右侧是上下文敏感的解释面，不是永久聊天栏。用户开始手动编辑时，回看流程自动暂停。

归档后，编辑点默认从正文界面消失，不再进入待处理列表，也不自动作为未来上下文；但对话、解释和修改历史仍保留。

## 11. 变更批次

一次委派对应一个变更批次，记录目标、授权范围、Thread、Session、修改文件和位置、编辑点关联、修改原因、原文与新文、时间和冲突状态。

变更展示不应只有 Diff，还应说明：

1. 改了什么；
2. 为什么改；
3. 有什么影响或代价。

支持按编辑点回看（默认）和按文件回看。跨文件修改应作为一个完整批次呈现。

## 12. 多文件与冲突

Thread 可以在多个文件之间维持阅读连续性，Agent 的跨文件结论必须回到原编辑点位置，而不是只出现在全局聊天窗口。

同一文件被多个 Thread 或人和 Agent 同时修改时：

- 委派基于明确文件版本；
- 版本未变化时应用；
- 版本变化时暂停；
- 不安全合并时不静默覆盖；
- 提供重新处理、独立候选、差异查看和手动协调。

## 13. 界面方向

当前默认结构：

```text
左侧：Thread 列表
中间：正文
右侧：当前上下文 / 编辑点 / Agent 解释
```

普通阅读时正文保持干净，右侧收窄或显示少量上下文；创建编辑点后正文边缘出现克制的小标记；Agent 工作时显示当前任务和进度；完成后进入解释优先的轻量回看。

右侧面板不是固定 AI 面板，而是随状态变化的 Context Surface：普通阅读、选区、Agent 工作和回看分别显示不同内容；无任务时尽量收起。

## 14. 文件与配置

真实文件永远是 source of truth。Lumir 状态可在本地配置目录中保存：

```text
~/.config/lumir/
  workspaces/
  threads/
  sessions/
  editor-points/
  change-batches/
  indexes/
  agent-memory/
```

状态和配置应可读、可备份、可导出、可恢复、可重建索引，并且不依赖云端。删除 Lumir 后，用户文件仍完整可用。

配置原则：

> **配置是数据，不是程序。**

配置错误不能阻止启动；损坏配置应自动隔离，并可恢复默认或上一个可用版本。

## 15. Agent 接入方向

Lumir 的统一 Agent 能力应包含当前选区、文件、Thread brief、阅读集合、编辑点、权限和委派范围；Agent 应返回解释、修改、原因、影响、进度、未解决问题和请求。

未来可通过 CLI、ACP 或类似协议接入外部 Agent，例如：

```text
lumir context
lumir ask
lumir propose
lumir apply
lumir watch
lumir status
```

重点不是让 Agent 能写文件，而是让它理解 Thread、Session、编辑点、原文位置、用户意图和变更批次。

## 16. 性能与扩展边界

第一版可以暂时不达到极致速度，但必须保护本地编辑循环：输入、选择、标记、导航、撤销和阅读不应被 Agent 阻塞；索引、分析和 Agent 任务异步运行。

未来扩展应遵守：

- 扩展不能阻塞输入；
- Agent 进程崩溃不拖垮编辑器；
- 权限明确声明；
- 失败自动降级；
- 后台能力可诊断；
- 性能是扩展 API 的契约。

## 17. MVP

第一版用于验证 Agent 协作方式，而不是做完整编辑器。

### 必须支持

1. 打开本地目录；
2. Thread 创建、切换和本地持久化；
3. Thread 关联多个文件；
4. Thread 无主文档也成立；
5. Markdown 编辑与预览 / Live Preview；
6. 纯文本和常见代码文件着色；
7. 选中文本创建编辑点；
8. 编辑点多轮对话；
9. 多个编辑点一次性委派；
10. 同一 Thread 继续当前 Session；
11. Agent 解释并直接修改正文；
12. 解释优先的逐点回看；
13. 变更批次记录；
14. 编辑点归档；
15. 基本撤销；
16. 外部文件修改检测；
17. Thread、编辑点和变更记录本地持久化；
18. 一个高完成度默认主题；
19. 一个主要 Agent 接入。

### 后置能力

- 自动持续阅读；
- 后台主动整理；
- 多 Agent 协商；
- 图谱；
- 插件市场；
- 云同步；
- 社交协作；
- 团队功能；
- 多设备协同；
- LSP；
- 调试器；
- 完整主题系统；
- 大规模文件空间优化；
- 完整 Obsidian 兼容性。

## 18. 第一版要验证的假设

1. Thread 是否比单纯文件列表更适合持续工作；
2. 用户是否愿意在一个 Thread 中长期维护工作目标；
3. 原地创建编辑点是否比复制到外部 Agent 更自然；
4. 用户是否会在阅读中积累多个编辑点；
5. 解释优先、直接修改正文是否可信；
6. 变更批次是否足够可回顾和撤销；
7. Thread brief 是否能恢复协作上下文；
8. 新 Thread 与旧 Thread 的边界是否清楚；
9. 多文件阅读集合是否比手动切换更有价值；
10. 跨文件结论能否可靠回到原文；
11. 用户是否愿意将 Agent 视为熟悉自己的编辑。

## 19. 设计原则

1. **默认安静，委派明确，返回可解释。**
2. 人不需要搬运上下文给 Agent。
3. 人负责意图与判断，Agent 负责执行与解释。
4. 文件比 Lumir 更长寿。
5. 授权优先于修改大小。
6. 解释帮助人理解，不替修改辩护。
7. 复杂性可以存在，但不能倾倒给用户。
8. 安全地拒绝优于聪明地覆盖。
9. 配置不能成为障碍。
10. 性能是生态契约。

## 20. 定位句候选

### 完整版

> Lumir 是一个把 Agent 变成可见协作者的本地文本工作台。你可以在真实文件中阅读、留下编辑意图，并与熟悉你的 Agent 共同演化正文。

### 差异化版

> 不是 AI 聊天，不是知识库，而是和 Agent 一起思考、写作、整理文件的工作台。

### 核心体验版

> 阅读中的每一次卡顿，都是一次让文本变好的机会。

### 节奏版

> 默认安静，委派明确，返回可解释。

## 21. 最终产品命题

> **Lumir 不是给文本加一个 Agent，而是重新设计人和 Agent 共同形成文本的过程。**

核心结构：

```text
文件是事实空间
Thread 是意图空间
Session 是 Agent 的上下文空间
编辑点是一次性认知事务
变更批次是协作执行记录
```
